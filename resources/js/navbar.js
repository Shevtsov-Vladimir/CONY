import Vue from "vue";

// require("./bootstrap");

// Vue.mixin({
    // methods: {
        // route: route,
    // },
// });

// urlPath = window.location.href;
// console.log(urlPath);

const catalog = new Vue({
    el: "#navbar",

    
});
